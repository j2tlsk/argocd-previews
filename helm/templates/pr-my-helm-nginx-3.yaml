---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  finalizers:
  - resources-finalizer.argocd.argoproj.io
  name: pr-my-helm-nginx-3
  namespace: argocd
spec:
  destination:
    namespace: pr-my-helm-nginx-3
    server: https://kubernetes.default.svc
  project: previews
  source:
    helm:
      values: |
        image:
          tag: "stable"
        ingress:
          host: "pr-my-helm-nginx-3.localhost"
      version: v3
    path: helm
    repoURL: https://github.com/j2tlsk/my-helm-nginx.git
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
